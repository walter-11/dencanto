# 🏗️ ARQUITECTURA DEL SISTEMA DE COTIZACIONES

## 📐 Diagrama General de Componentes

```
┌──────────────────────────────────────────────────────────────────────┐
│                     CLIENTE (NAVEGADOR WEB)                          │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────┐         ┌──────────────────┐                   │
│  │  /productos     │         │ /carrito         │                   │
│  │  (Catálogo)     │────────→│ (Formulario)     │                   │
│  └────────┬────────┘         └────────┬─────────┘                   │
│           │                           │                             │
│    "Ver detalles"               "Enviar Cotización"                 │
│    Modal con producto           (validar + enviar)                  │
│           │                           │                             │
│           ▼                           ▼                             │
│  ┌──────────────────────────────────────────┐                      │
│  │        localStorage (carrito.js)        │                      │
│  │  [{id, nombre, cantidad, precio}, ...] │                      │
│  └──────────────────────────────────────────┘                      │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  │
                  HTTP POST /carrito/api/enviar-cotizacion
                  Content-Type: application/json
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│              SERVIDOR (SPRING BOOT 3.3.7 - JAVA 21)                 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │        CarritoCotizacionesController                       │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  POST /carrito/api/enviar-cotizacion                       │    │
│  │    ├─ @RequestBody @Valid Cotizacion                      │    │
│  │    ├─ Validar (Jakarta Validation)                        │    │
│  │    └─ return JSON {success: true/false}                   │    │
│  └────────────────────┬─────────────────────────────────────┘    │
│                       │                                             │
│                       ▼                                             │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │        CotizacionService (@Service)                        │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  + guardar(Cotizacion): Cotizacion                         │    │
│  │  + obtenerPorId(Integer): Cotizacion                       │    │
│  │  + obtenerTodas(): List<Cotizacion>                        │    │
│  │  + obtenerPorEmail(String): List<Cotizacion>              │    │
│  │  + actualizarEstado(Integer, String): void                │    │
│  │  + contarPorEstado(String): Long                           │    │
│  │  + ... más métodos                                         │    │
│  └────────────────────┬─────────────────────────────────────┘    │
│                       │                                             │
│                       ▼                                             │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │    CotizacionRepository extends JpaRepository             │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  + findByEstado(String): List<Cotizacion>                 │    │
│  │  + findByEmail(String): List<Cotizacion>                  │    │
│  │  + findByFechaCreacionBetween(...): List<Cotizacion>     │    │
│  │  + countByEstado(String): Long                            │    │
│  └────────────────────┬─────────────────────────────────────┘    │
│                       │                                             │
│                       ▼                                             │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │  Cotizacion (@Entity, @Table(name="cotizaciones"))        │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  - id: Integer (@Id @GeneratedValue)                      │    │
│  │  - nombreCliente: String (@NotBlank @Size)                │    │
│  │  - email: String (@Email)                                 │    │
│  │  - telefono: String (@Pattern)                            │    │
│  │  - direccion: String (@Size)                              │    │
│  │  - fechaDeseada: LocalDate                                │    │
│  │  - productosJson: String (JSON)                           │    │
│  │  - total: Double (@Min)                                   │    │
│  │  - estado: String (Pendiente/Procesando/Completado)      │    │
│  │  - fechaCreacion: LocalDateTime (@CreationTimestamp)     │    │
│  │  - fechaActualizacion: LocalDateTime (@UpdateTimestamp)  │    │
│  └────────────────────┬─────────────────────────────────────┘    │
│                       │                                             │
└───────────────────────┼─────────────────────────────────────────────┘
                        │
                        │
                        ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    BASE DE DATOS (MySQL 8.0)                        │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     TABLA: cotizaciones                     │   │
│  ├──────────────────────┬───────────────────────────────────────┤   │
│  │ id (PK)              │ INT AUTO_INCREMENT                   │   │
│  │ nombre_cliente       │ VARCHAR(100) NOT NULL                │   │
│  │ email                │ VARCHAR(100) NOT NULL                │   │
│  │ telefono             │ VARCHAR(20) NOT NULL                 │   │
│  │ direccion            │ VARCHAR(255) NOT NULL                │   │
│  │ fecha_deseada        │ DATE NOT NULL                        │   │
│  │ productos_json       │ JSON NOT NULL                        │   │
│  │ total                │ DECIMAL(10,2) NOT NULL              │   │
│  │ estado               │ VARCHAR(50) DEFAULT 'Pendiente'      │   │
│  │ fecha_creacion       │ TIMESTAMP (auto)                    │   │
│  │ fecha_actualizacion  │ TIMESTAMP (on update)               │   │
│  └──────────────────────┴───────────────────────────────────────┘   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                        ÍNDICES                              │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │  - idx_estado (para filtros)                               │   │
│  │  - idx_email (para búsquedas por cliente)                  │   │
│  │  - idx_fecha_creacion (para reportes)                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Usuario - Step by Step

```
PASO 1: VISUALIZAR CATÁLOGO
┌─────────────────────────────────────────┐
│ Usuario accede a /productos             │
│ Ver catálogo de colchones y accesorios  │
│ Bootstrap 5 responsive grid             │
└─────────┬───────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────┐
│ Usuario haz clic "Ver detalles"         │
│ Se abre MODAL con info completa         │
│ Carrusel de imágenes técnicas           │
│ Especificaciones del producto           │
└─────────┬───────────────────────────────┘
          │
PASO 2: AGREGAR AL CARRITO
          ▼
┌─────────────────────────────────────────┐
│ Usuario selecciona cantidad (1-99)      │
│ Haz clic "Agregar al Carrito"           │
│ scriptProductos.js → click handler      │
└─────────┬───────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────┐
│ carrito.js: agregarAlCarrito()          │
│ ├─ Verificar si existe en carrito      │
│ ├─ Si existe: sumar cantidad            │
│ ├─ Si no existe: agregar nuevo          │
│ └─ localStorage.setItem(...)            │
└─────────┬───────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────┐
│ UI Updates:                             │
│ ├─ mostrarNotificacion() → Toast ✓     │
│ ├─ actualizarBadgeCarrito() → "2"      │
│ └─ Cerrar modal                         │
└─────────┬───────────────────────────────┘
          │
PASO 3: VISUALIZAR CARRITO
          ▼
┌─────────────────────────────────────────┐
│ Usuario haz clic en badge del carrito   │
│ Se abre: /carrito/cotizaciones          │
│ Muestra:                                │
│  ├─ Resumen de productos                │
│  ├─ Total a pagar                       │
│  └─ Formulario de datos del cliente     │
└─────────┬───────────────────────────────┘
          │
PASO 4: COMPLETAR FORMULARIO
          ▼
┌─────────────────────────────────────────┐
│ Usuario completa 5 campos:              │
│ ├─ Nombre completo (3-100 chars)       │
│ ├─ Email (formato válido)              │
│ ├─ Teléfono (7-20 chars)               │
│ ├─ Dirección (5-255 chars)             │
│ └─ Fecha deseada (obligatoria)         │
└─────────┬───────────────────────────────┘
          │
PASO 5: VALIDAR EN CLIENTE
          ▼
┌─────────────────────────────────────────┐
│ formularioCotizacion.submit event       │
│ validarFormulario():                    │
│ ├─ nombre.length >= 3 ✓                │
│ ├─ email regex match ✓                 │
│ ├─ telefono.length 7-20 ✓             │
│ ├─ direccion.length >= 5 ✓            │
│ └─ fechaDeseada not null ✓            │
│                                         │
│ SI FALLAN: mostrarErrores(errores)     │
│ └─ Rojo debajo del campo                │
│ └─ return (no enviar)                   │
└─────────┬───────────────────────────────┘
          │
PASO 6: ENVIAR AL SERVIDOR
          ▼
┌─────────────────────────────────────────┐
│ fetch() POST /carrito/api/enviar...     │
│ Body: JSON completo                     │
│ {                                       │
│   "nombreCliente": "Juan",              │
│   "email": "juan@example.com",          │
│   "telefono": "+51 987 654 321",        │
│   "direccion": "Jr. Lima 123",          │
│   "fechaDeseada": "2024-12-20",         │
│   "productosJson": "[{...}]",           │
│   "total": 1500,                        │
│   "estado": "Pendiente"                 │
│ }                                       │
└─────────┬───────────────────────────────┘
          │
PASO 7: VALIDAR EN SERVIDOR
          ▼
┌─────────────────────────────────────────┐
│ CarritoCotizacionesController           │
│ @Valid Cotizacion                       │
│ Jakarta Validation:                     │
│ ├─ @NotBlank (todos)                   │
│ ├─ @Size (nombre, dirección)            │
│ ├─ @Email (email)                      │
│ └─ @Pattern (teléfono)                 │
│                                         │
│ SI FALLAN: return {                     │
│   "success": false,                     │
│   "detalles": {                         │
│     "email": "no es válido"             │
│   }                                     │
│ }                                       │
└─────────┬───────────────────────────────┘
          │
PASO 8: GUARDAR EN BD
          ▼
┌─────────────────────────────────────────┐
│ SI VÁLIDO:                              │
│ CotizacionService.guardar()             │
│ ├─ Crear entidad Cotizacion             │
│ ├─ Establece estados/timestamps         │
│ ├─ CotizacionRepository.save()          │
│ └─ MySQL: INSERT INTO cotizaciones      │
│                                         │
│ return {                                │
│   "success": true,                      │
│   "message": "Cotización guardada",     │
│   "id": 123                             │
│ }                                       │
└─────────┬───────────────────────────────┘
          │
PASO 9: ACTUALIZAR UI (Éxito)
          ▼
┌─────────────────────────────────────────┐
│ Cliente recibe respuesta JSON           │
│ if (result.success):                    │
│ ├─ localStorage.removeItem() → vaciar   │
│ ├─ Mostrar alerta verde ✓               │
│ ├─ cargarCarrito() → vacío               │
│ └─ setTimeout → redirect a / (3s)       │
└─────────────────────────────────────────┘
```

---

## 📱 Componentes Frontend

```
┌──────────────────────────────────────────┐
│          /carrito/cotizaciones.html      │
├──────────────────────────────────────────┤
│                                          │
│  ┌─ HEADER ─────────────────────────┐  │
│  │ Logo, Navbar, Links, Carrito     │  │
│  └──────────────────────────────────┘  │
│                                          │
│  ┌─ MAIN CONTAINER ──────────────────┐  │
│  │                                   │  │
│  │  ┌─ Encabezado ──────────────┐   │  │
│  │  │ "🛒 Carrito de Cotizaciones│   │  │
│  │  └───────────────────────────┘   │  │
│  │                                   │  │
│  │  ┌─ Alerta (éxito/error) ────┐   │  │
│  │  │ id="alertContainer"        │   │  │
│  │  └───────────────────────────┘   │  │
│  │                                   │  │
│  │  ┌─ Resumen de Productos ───┐   │  │
│  │  │ id="productosResumen"     │   │  │
│  │  │ └─ id="productosList"     │   │  │
│  │  │   [Lista dinámica]        │   │  │
│  │  └───────────────────────────┘   │  │
│  │                                   │  │
│  │  ┌─ Total ───────────────────┐   │  │
│  │  │ S/ <span id="totalCotiz..>│   │  │
│  │  └───────────────────────────┘   │  │
│  │                                   │  │
│  │  ┌─ FORMULARIO ───────────────┐  │  │
│  │  │ id="formularioCotizacion"  │  │  │
│  │  │                            │  │  │
│  │  │  ├─ nombreCliente          │  │  │
│  │  │  │  └─ error-nombreCliente │  │  │
│  │  │  │                         │  │  │
│  │  │  ├─ email                  │  │  │
│  │  │  │  └─ error-email         │  │  │
│  │  │  │                         │  │  │
│  │  │  ├─ telefono               │  │  │
│  │  │  │  └─ error-telefono      │  │  │
│  │  │  │                         │  │  │
│  │  │  ├─ direccion              │  │  │
│  │  │  │  └─ error-direccion     │  │  │
│  │  │  │                         │  │  │
│  │  │  ├─ fechaDeseada           │  │  │
│  │  │  │  └─ error-fechaDeseada  │  │  │
│  │  │  │                         │  │  │
│  │  │  └─ btnEnviar              │  │  │
│  │  │     ├─ btnText             │  │  │
│  │  │     └─ btnSpinner          │  │  │
│  │  │                            │  │  │
│  │  └────────────────────────────┘  │  │
│  │                                   │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ┌─ FOOTER ─────────────────────────┐  │
│  │ Empresa, Enlaces, Contacto       │  │
│  └──────────────────────────────────┘  │
│                                          │
└──────────────────────────────────────────┘
```

---

## 🗂️ Estructura de Archivos

```
dencanto/
├── pom.xml
├── mvnw / mvnw.cmd
│
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/proyecto/dencanto/
│   │   │       ├── model/
│   │   │       │   └── Cotizacion.java ✨ NEW
│   │   │       ├── repository/
│   │   │       │   └── CotizacionRepository.java ✨ NEW
│   │   │       ├── service/
│   │   │       │   └── CotizacionService.java ✨ NEW
│   │   │       └── controller/
│   │   │           ├── ProductoController.java (modificado)
│   │   │           └── CarritoCotizacionesController.java ✨ NEW
│   │   │
│   │   └── resources/
│   │       ├── templates/
│   │       │   ├── productos.html (modificado)
│   │       │   └── carrito/
│   │       │       └── cotizaciones.html ✨ NEW
│   │       └── static/
│   │           ├── css/
│   │           │   ├── productos.css
│   │           │   └── ...
│   │           └── js/
│   │               ├── carrito.js ✨ NEW
│   │               ├── scriptProductos.js (modificado)
│   │               └── ...
│   │
│   └── test/
│       └── ...
│
├── target/
│   ├── classes/ (compilado)
│   └── dencanto-0.0.1-SNAPSHOT.jar
│
├── crear_tabla_cotizaciones.sql ✨ NEW
├── SISTEMA_COTIZACIONES_COMPLETO.md ✨ NEW
├── CHECKLIST_COTIZACIONES.md ✨ NEW
├── COMANDOS_RAPIDOS.md ✨ NEW
└── RESUMEN_TECNICO_COTIZACIONES.md ✨ NEW
```

---

## 📊 Matriz de Responsabilidades

| Componente | Responsabilidad |
|------------|-----------------|
| **productos.html** | Mostrar catálogo, abrir modales |
| **scriptProductos.js** | Manejar clics, agregar al carrito |
| **carrito.js** | Persistir en localStorage, cálculos |
| **cotizaciones.html** | Mostrar formulario, validación UI |
| **CarritoCotizacionesController** | Recibir POST, validar, enviar respuesta |
| **CotizacionService** | Lógica de negocio, guardar BD |
| **CotizacionRepository** | Acceso a datos, queries |
| **Cotizacion Entity** | Definir estructura, validaciones |
| **MySQL** | Almacenar datos persistentes |

---

## 🔄 Ciclo de Vida de una Cotización

```
ESTADO INICIAL: (Cliente Crea)
  ├─ Status: "Pendiente"
  ├─ fecha_creacion: NOW()
  ├─ fecha_actualizacion: NOW()
  └─ productos_json: [...]

ESTADO INTERMEDIO: (Admin Procesa)
  ├─ Status: "Procesando"
  └─ fecha_actualizacion: NOW()

ESTADO FINAL: (Admin Completa)
  ├─ Status: "Completado"
  └─ fecha_actualizacion: NOW()

CONSULTAS ÚTILES:
  - SELECT * WHERE estado='Pendiente' ORDER BY fecha_creacion
  - SELECT * WHERE estado='Procesando' ORDER BY fecha_actualizacion
  - SELECT * WHERE fecha_creacion BETWEEN '2024-01-01' AND '2024-12-31'
  - SELECT * WHERE email='cliente@example.com'
```

---

## 🎯 Matriz de Validación

```
┌─────────────┬──────────────────┬──────────┬────────────┐
│ Campo       │ Validaciones     │ Cliente  │ Servidor   │
├─────────────┼──────────────────┼──────────┼────────────┤
│ nombre      │ @NotBlank        │ ✓        │ ✓          │
│             │ @Size(3, 100)    │ ✓        │ ✓          │
├─────────────┼──────────────────┼──────────┼────────────┤
│ email       │ @NotBlank        │ ✓        │ ✓          │
│             │ @Email           │ ✓ (regex)│ ✓ (Jakarta)│
├─────────────┼──────────────────┼──────────┼────────────┤
│ telefono    │ @NotBlank        │ ✓        │ ✓          │
│             │ @Pattern(regex)  │ ✓        │ ✓          │
├─────────────┼──────────────────┼──────────┼────────────┤
│ direccion   │ @NotBlank        │ ✓        │ ✓          │
│             │ @Size(5, 255)    │ ✓        │ ✓          │
├─────────────┼──────────────────┼──────────┼────────────┤
│ fechaDeseada│ @NotNull         │ ✓        │ ✓          │
├─────────────┼──────────────────┼──────────┼────────────┤
│ productosJson│ JSON array       │ ✓ (check)│ ✓ (guardar)│
├─────────────┼──────────────────┼──────────┼────────────┤
│ total       │ @Min(0)          │ ✓ (calc) │ ✓ (check)  │
└─────────────┴──────────────────┴──────────┴────────────┘
```

---

**Arquitectura lista para despliegue en producción ✅**
